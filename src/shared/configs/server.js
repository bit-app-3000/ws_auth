export const WS_OPTS = {
  pathname: '/',
  compression: true,
  transformer: 'websockets',
  parser: 'JSON',
  port:9595,
  // maxLength: 10485760,
  // methods: 'GET',
  // credentials: true,
  // exposed: true
}
